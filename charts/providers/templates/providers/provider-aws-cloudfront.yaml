apiVersion: pkg.crossplane.io/v1
kind: Provider
metadata:
  name: {{ include "crossplane-providers.fullname" . }}-aws-cloudfront
  labels:
    {{- include "crossplane-providers.labels" . | nindent 4 }}
spec:
  package: {{ include "crossplane-providers.packageUrl" (dict "Values" .Values "providerName" "cloudfront") }}
  {{- if .Values.aws.providers.cloudfront.controllerConfig }}
  controllerConfigRef:
    name: {{ .Values.aws.providers.cloudfront.controllerConfig.name }}
  {{- end }}
