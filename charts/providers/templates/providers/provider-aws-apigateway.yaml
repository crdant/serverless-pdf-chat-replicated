apiVersion: pkg.crossplane.io/v1
kind: Provider
metadata:
  name: {{ include "crossplane-providers.fullname" . }}-aws-apigateway
  labels:
    {{- include "crossplane-providers.labels" . | nindent 4 }}
spec:
  package: xpkg.upbound.io/upbound/provider-aws-apigateway:{{ .Values.aws.providers.apigateway.version }}
  {{- if .Values.aws.providers.apigateway.controllerConfig }}
  controllerConfigRef:
    name: {{ .Values.aws.providers.apigateway.controllerConfig.name }}
  {{- end }}
