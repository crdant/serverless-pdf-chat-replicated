apiVersion: pkg.crossplane.io/v1
kind: Provider
metadata:
  name: upbound-provider-aws-secretsmanager
  labels:
    {{- include "crossplane-providers.labels" . | nindent 4 }}
spec:
  package: xpkg.upbound.io/upbound/provider-aws-secretsmanager:{{ .Values.aws.providers.secretsmanager.version }}
  {{- if .Values.aws.providers.secretsmanager.controllerConfig }}
  controllerConfigRef:
    name: {{ .Values.aws.providers.secretsmanager.controllerConfig.name }}
  {{- end }}
