apiVersion: pkg.crossplane.io/v1
kind: Provider
metadata:
  name: {{ include "crossplane-providers.fullname" . }}-aws-cognitoidp
  labels:
    {{- include "crossplane-providers.labels" . | nindent 4 }}
spec:
  package: {{ include "crossplane-providers.packageUrl" (dict "Values" .Values "providerName" "cognitoidp") }}
  {{- if .Values.aws.providers.cognitoidp.controllerConfig }}
  controllerConfigRef:
    name: {{ .Values.aws.providers.cognitoidp.controllerConfig.name }}
  {{- end }}
