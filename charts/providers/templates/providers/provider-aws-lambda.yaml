apiVersion: pkg.crossplane.io/v1
kind: Provider
metadata:
  name: {{ include "providers.fullname" . }}-aws-lambda
  labels:
    {{- include "providers.labels" . | nindent 4 }}
spec:
  package: {{ include "providers.packageUrl" (dict "Values" .Values "providerName" "lambda") }}
  {{- if .Values.aws.providers.lambda.controllerConfig }}
  controllerConfigRef:
    name: {{ .Values.aws.providers.lambda.controllerConfig.name }}
  {{- end }}
