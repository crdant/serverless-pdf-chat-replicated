apiVersion: pkg.crossplane.io/v1
kind: Provider
metadata:
  name: upbound-provider-aws-cognito
  labels:
    {{- include "crossplane-providers.labels" . | nindent 4 }}
spec:
  package: xpkg.upbound.io/upbound/provider-aws-cognito:{{ .Values.aws.providers.cognito.version }}
  {{- if .Values.aws.providers.cognito.controllerConfig }}
  controllerConfigRef:
    name: {{ .Values.aws.providers.cognito.controllerConfig.name }}
  {{- end }}
