apiVersion: pkg.crossplane.io/v1
kind: Provider
metadata:
  name: {{ include "crossplane-providers.fullname" . }}-aws-sqs
  labels:
    {{- include "crossplane-providers.labels" . | nindent 4 }}
spec:
  package: {{ include "crossplane-providers.packageUrl" (dict "Values" .Values "providerName" "sqs") }}
  {{- if .Values.aws.providers.sqs.controllerConfig }}
  controllerConfigRef:
    name: {{ .Values.aws.providers.sqs.controllerConfig.name }}
  {{- end }}
